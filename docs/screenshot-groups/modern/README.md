# Modern-state screenshots

This directory contains the implemented Group 6 interface using the same 13-shot sequence as `original` and `restored`.

All images come from the isolated `--demo` profile. They contain synthetic media, generated artwork, no credentials, and no personal library paths.

The sequence covers the six libraries, element selector, movie/folder/game forms, sort menu, metadata-search state, and synthetic movie details.
