# Archive reference

The immutable recovery archive and historical workspace are deliberately not stored in Git.

This directory will contain textual provenance and migration notes only. It must not contain backup projects, generated binaries, private databases, or copyrighted sample libraries.

